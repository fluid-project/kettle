{
    "typeName": "kettle.tests.CORS_originConfig",
    "options": {
        "gradeNames": ["fluid.component"],
        "distributeOptions": {
            "record": "@expand:fluid.getGlobalValue(kettle.tests.testCORSOrigin)",
            "target": "{that middleware CORS}.options.origin"
        }
    },
    "includes": ["./CORS.json"]
}
