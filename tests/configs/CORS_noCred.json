{
    "typeName": "CORS_noCred",
    "options": {
        "gradeNames": ["fluid.component"],
        "distributeOptions": {
            "record": false,
            "target": "{that middleware CORS}.options.credentials"
        }
    },
    "includes": ["./CORS.json"]
}
