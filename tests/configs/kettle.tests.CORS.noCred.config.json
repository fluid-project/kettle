{
    "typeName": "kettle.tests.CORS.noCred.config",
    "options": {
        "gradeNames": ["fluid.component"],
        "distributeOptions": {
            "record": false,
            "target": "{that middleware CORS}.options.credentials"
        }
    },
    "includes": ["./kettle.tests.CORS.config.json"]
}
