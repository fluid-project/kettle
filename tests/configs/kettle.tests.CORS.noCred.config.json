{
    "type": "kettle.tests.CORS.noCred.config",
    "options": {
        "gradeNames": ["fluid.component"],
        "distributeOptions": {
            "record": false,
            "target": "{that middlewareHolder CORS}.options.credentials"
        }
    },
    "includes": ["./kettle.tests.CORS.config.json"]
}
